[package]
name = "mlx_runtime_gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
mlx_runtime_protocol = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
